import {Grid} from "@mui/material";

export default function DashboardPage() {

    return (
        <Grid container spacing={2}>
            <Grid item xs={12}>
                Hello World!
            </Grid>
        </Grid>
    )
}
